aws_region          = "ap-southeast-1"
aws_profile         = "Academy"
application_service = "JewelCart"
cost_centre         = "<replace-me>"

# After import, run `terraform plan` and copy every setting reported as drift
# from AWS into the matching map in your real terraform.tfvars.
